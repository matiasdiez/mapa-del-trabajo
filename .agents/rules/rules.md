# Package Management Rules
* Always use `pnpm` for installing dependencies, running scripts, and managing the project.
* Do NOT use `npm`, `yarn`, or `bun` under any circumstances.
* Example: Use `pnpm add <package>` instead of `npm install <package>`.

